<h1>Books</h1>
<%= form_with model: @book.new do |f| %>

<!--投稿一覧を表示する-->
<% @books.each do |list| %>
<p>title</p><span><%= book.title %></span>
<% end %>

<!--投稿機能-->
<h2>New book</h2>
<h4>title</h4>
<%= f.text_field :title %>

<h4>body</h4>
<%= f.text_area :body %>

<%= f.submit 'create Book'%>
<% end %>
